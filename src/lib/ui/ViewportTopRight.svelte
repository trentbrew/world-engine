<script lang="ts">
	/**
	 * Play-mode top-right float slot.
	 * Camera / stats / jank live top-left in the doc bar during play.
	 * Kept mounted so the chrome slot can return without a rebuild.
	 */
</script>

<!-- Intentionally empty — reserved top-right viewport chrome. -->
