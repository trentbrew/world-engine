/** Phase 1 stub — future tweakcn paste import (Phase 1.1). */
export type ParsedTweakcnTheme = {
	id: string;
	vars: Record<string, string>;
};

export function parseTweakcnBlock(_source: string): ParsedTweakcnTheme | null {
	return null;
}
