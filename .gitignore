node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Transient build/check logs
*.exit
.*.log
.*.txt
build-stderr.txt

# Agent scratch / test artifacts (never commit)
.agent/
.agents/
.playwright-mcp/
docs/scratch/
/test-results/

# Scratch logs & debug screenshots (root level)
/*.log
/*.txt

# Separate git repo — canonical source is github.com/turtlehq/worlds
/worlds-registry/
/*.png
/*.jpeg
/*.out
/pw-*
/review-*
/check-*
/e2e-run.log

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.trellis/
!.trellis/tests.json
!.trellis/browser-suites/

# Local Trellis durable DB — contains an API key / JWT secret; never commit
.trellis-db/
.trellis-db.json

# Sprite deploy record (writes .trellis-db.json with live apiKey/jwtSecret)
.trellis-deploy/

# Local realtime-relay blob store (content-addressed uploads; regenerable)
.trellis-relay/
